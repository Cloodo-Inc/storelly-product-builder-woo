/*!
 * Chart.js placeholder — replace with the real Chart.js v4.x UMD build
 * before shipping the marketplace Report dashboard charts.
 *
 *   Recommended source: https://github.com/chartjs/Chart.js/releases
 *     tag v4.4.0 → dist/chart.umd.min.js  (MIT License)
 *
 * Without the real library, SPBWC_Marketplace_Admin still enqueues this
 * file, the report dashboard cards render normally, and the new Chart()
 * calls in static/js/marketplace-admin.js are skipped by the
 *   `typeof window.Chart === 'undefined'`
 * guard. So the admin remains functional; only the two line/bar charts
 * are absent.
 */
