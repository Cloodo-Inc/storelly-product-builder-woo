<?xml version="1.0"?>
<ruleset name="Storelly Product Builder for WooCommerce">
	<description>PHPCS ruleset — security-focused check aligned with wordpress.org review guidelines.</description>

	<!-- Only scan PHP; skip generated, vendor, tool and data files. -->
	<arg name="extensions" value="php"/>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>tools/*</exclude-pattern>
	<exclude-pattern>storage/*</exclude-pattern>

	<!-- Base on WordPress standard. -->
	<rule ref="WordPress"/>

	<!-- ── Custom capabilities ──────────────────────────────────────── -->
	<rule ref="WordPress.WP.Capabilities">
		<properties>
			<property name="custom_capabilities" type="array">
				<element value="spbwc_manage_product_builder"/>
			</property>
		</properties>
	</rule>

	<!-- ── File naming ──────────────────────────────────────────────── -->
	<!--
	  The plugin uses class-{name}.php without repeating the full spbwc_ prefix
	  in every filename — consistent with the existing codebase's convention.
	  Strict class-to-filename mapping is therefore disabled.
	-->
	<rule ref="WordPress.Files.FileName">
		<properties>
			<property name="strict_class_file_names" value="false"/>
		</properties>
	</rule>

	<!-- ── PHPDoc comments ──────────────────────────────────────────── -->
	<!--
	  The pre-existing codebase pre-dates the WP coding-standards doc-comment
	  requirement. These sniffs produce ~30 k noise errors across legacy files
	  and are NOT enforced by the wordpress.org plugin review team for
	  functionality / security review.  Exclude them project-wide; they should
	  be addressed in a dedicated cleanup pass.
	-->
	<rule ref="Squiz.Commenting.ClassComment" />
	<rule ref="Squiz.Commenting.ClassComment">
		<exclude name="Squiz.Commenting.ClassComment.Missing"/>
	</rule>
	<rule ref="Squiz.Commenting.FunctionComment">
		<exclude name="Squiz.Commenting.FunctionComment.Missing"/>
		<exclude name="Squiz.Commenting.FunctionComment.MissingParamComment"/>
		<exclude name="Squiz.Commenting.FunctionComment.MissingParamTag"/>
		<exclude name="Squiz.Commenting.FunctionComment.ParamCommentFullStop"/>
		<exclude name="Squiz.Commenting.FunctionComment.SpacingAfterParamType"/>
	</rule>
	<rule ref="Squiz.Commenting.VariableComment">
		<exclude name="Squiz.Commenting.VariableComment.Missing"/>
	</rule>
	<rule ref="Squiz.Commenting.FileComment">
		<exclude name="Squiz.Commenting.FileComment.Missing"/>
		<exclude name="Squiz.Commenting.FileComment.MissingShort"/>
	</rule>
</ruleset>
